05. 리액트에서 CSS 사용하기

1. 컴포넌트를 구성하는 리액트에서는 
일반적으로 CSS 파일을 import하여 사용함

2. import 한 CSS 파일을 html페이지에 직접 CSS구역으로 배포되며 이것은 인터널 방식이다.

3. CSS공통 파일 합치기
-> 문법 : @import() 
보통 CSS에서는 윗쪽에 쓰면 위에 들어오는데 
리액트는 반대로 아래에 추가로 style구역이 만들어지며 추가된다.
